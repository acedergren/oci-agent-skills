{
  "mcpServers": {
    "oci-api": {
      "description": "Oracle Cloud Infrastructure API access via MCP",
      "disabled": false
    },
    "context7": {
      "description": "Up-to-date documentation retrieval for OCI CLI and services",
      "disabled": false
    },
    "oracle-oci-pricing": {
      "description": "Real-time OCI pricing lookups by SKU or product name with currency conversion (requires local oracle/mcp installation)",
      "disabled": true,
      "_comment": "To enable: Clone oracle/mcp repo and update command path. See docs/MCP_SETUP.md"
    },
    "oracle-oci-usage": {
      "description": "Cost and usage analytics for FinOps automation (requires local oracle/mcp installation)",
      "disabled": true,
      "_comment": "To enable: Clone oracle/mcp repo and update command path. See docs/MCP_SETUP.md"
    },
    "oracle-oci-resource-search": {
      "description": "Cross-compartment and cross-service resource discovery (requires local oracle/mcp installation)",
      "disabled": true,
      "_comment": "To enable: Clone oracle/mcp repo and update command path. See docs/MCP_SETUP.md"
    },
    "oracle-oci-cloud-guard": {
      "description": "Cloud Guard security issues and recommendations (requires local oracle/mcp installation)",
      "disabled": true,
      "_comment": "To enable: Clone oracle/mcp repo and update command path. See docs/MCP_SETUP.md"
    }
  }
}
